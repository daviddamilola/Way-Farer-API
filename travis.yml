language: node_js
services:
  - postgresql
node_js:
  - "stable"
notifications:
  email: false
cache:
  directories:
    - node_modules
before_script:
  - npm install
  - psql -c 'create database travis_ci_test;' -U postgres
script:
  - npm run test
after_success:
  - npm run coverage
  